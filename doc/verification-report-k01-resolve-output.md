# Verifikasi output dan pemulihan RESOLVE K01

Dokumen ini mencatat pembuktian batas EXTRACT–registry CAS–artefak RESOLVE pada PostgreSQL disposable. Cakupannya adalah komit keputusan yang sudah diberi kandidat/proposal/review terpin; dokumen ini bukan klaim bahwa seluruh K01 atau benchmark release selesai.

Revision fitur `bd17a4b` di atas baseline `bf0235cf8abd0c135d870a66a6a0f34b3d8308bb`, diuji pada 2026-09-24 dengan Go 1.26.8 windows/amd64 dan PostgreSQL 18.0. Migration baru `0010_semantic_resolution_intents.up.sql` memiliki SHA-256 `b8354f3b30d985b22f2caea53e416e60cc065ec922f8c49022bc27455fa27a58`; tes memakai database disposable `regulagraph_k01i` agar checksum migration yang pernah diaplikasikan tidak disunting diam-diam.

Input yang diuji adalah fixture `TestSemanticRegistryAgainstPostgres` dan `TestEmptySemanticResolutionAgainstPostgres` dalam `registry_semantic_integration_test.go`: EXTRACT lengkap dengan/ tanpa mention, kandidat terdaftar, review LINK terikat hash, lease/fence dan checkpoint EXTRACT. Output yang diharapkan adalah satu receipt registry, intent immutable sebelum CAS, `ResolutionBatch` terikat EXTRACT/kandidat, manifest dependency, checkpoint terminal, dan pemulihan byte-identik pada retry. Skenario tandingan mencakup identitas output invalid sebelum CAS, kegagalan penyimpanan artefak setelah CAS, perubahan input retry, crash setelah checkpoint, serta identitas output yang berubah saat recovery kosong.

`go test ./... -count=1` dari `src/server` dengan `REGULAGRAPH_TEST_POSTGRES_DSN` menuju database disposable: **PASS**, exit 0. Log mentah berada di `artifacts/verification/k01-resolve-output-20260924/go-test-all.log` (SHA-256 `67fdb5eadf756895c98d0e56000407aa49b700d3da19a0fca7bde66d9b7dff09`). `go vet ./...`: **PASS**, exit 0; stdout/stderr kosong dan exit code dicatat di `go-vet.log` pada folder run yang sama. `git diff --check`: **PASS**, exit 0. Reviewer independen melakukan review baca-saja atas pre-CAS/replay, guard EXTRACT kosong, identitas recovery sebelum side effect, dan dokumentasi: **PASS terbatas**, tanpa blocker lokal setelah laporan/log tersedia. Reviewer tidak menjalankan PostgreSQL sendiri.

**Batas terbuka:** kandidat/proposal belum diproduksi dan di-dispatch dari workflow produksi; identitas reviewer masih harus diautentikasi oleh producer. Intent yang terlanjur tersimpan lalu gagal CAS karena perubahan registry tidak boleh diedit; coordinator perlu memberi terminal failure/replan job baru. Graph assembly, kualitas resolusi pada gold hukum, latency p95/p99, throughput, serta 59 gate required release masih **NOT_MEASURED/BLOCKED** sesuai prasyaratnya. Fixture ini membuktikan invariant lokal dan recovery, bukan akurasi legal atau SLA.
